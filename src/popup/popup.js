const tooltipStatus = document.querySelector("#status");
tooltipStatus.textContent = "Здесь будет что-то важное...";

// const response = await chrome.runtime.sendMessage({msg: "is_tt"});

// if (response.status) tooltipStatus.textContent = "Tooltips in enabled for this website";
// else tooltipStatus.textContent = "Tooltips in disabled for this website";